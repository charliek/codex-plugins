interface:
  display_name: "Deploy Build"
  short_description: "Create and push a date-based deployment release"
  default_prompt: "Use $build to create a date-based deployment release."
policy:
  allow_implicit_invocation: false
