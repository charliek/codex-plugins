interface:
  display_name: "Release"
  short_description: "Cut a convention-based semantic version release"
  default_prompt: "Use $release to cut the requested semantic version release."
policy:
  allow_implicit_invocation: false
